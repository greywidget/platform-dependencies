I also solved this another way by making sets of the adjacent land for a square and iteratively merging any sets that had an intersection.

See `more_islands.py`

Also see `pybites_solution.py` for the simpler official solution
